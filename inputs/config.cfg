// -- Configuration file for MCMC
// -- Comments must be on their own line and begin with "//"
// -- Congigurations must be of the format "[NAME] = [VALUE]" (whitespace is important for parsing)

// -- File Settings
// Save output to
OUTPUT_DIRECTORY = "/scratch/summit/mire0265/outputs/"
// Specify run and npar in script
MCMC_FILE_BASE = "mcmc_"
COVMAT_FILE_BASE = "covariance_"
// If we are running from a previous chain, specify here
NEW_CHAIN = true
INPUT_DIRECTORY = "/scratch/summit/mire0265/outputs/"

// -- MCMC Settings
NSTEPS = 10000
// Global step set based on (2.38^2)/d
NPARS = 10
// Size of the regularization parameter applied to the diagonal of the proposal function
EPSILON = 0.05
// Current run number (repetition number)
RUN = 0
// Number of parallel chains to run
BRANCHES = 10

